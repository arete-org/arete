{
    "name": "@arete/backend",
    "private": true,
    "type": "module",
    "main": "dist/server.js",
    "scripts": {
        "build": "pnpm --dir ../.. --filter @arete/contracts run build && tsc -p tsconfig.json && node -e \"const fs=require('node:fs'); fs.mkdirSync('dist/services/prompts',{recursive:true}); fs.copyFileSync('src/services/prompts/defaults.yaml','dist/services/prompts/defaults.yaml');\""
    },
    "dependencies": {
        "@arete/contracts": "workspace:*",
        "better-sqlite3": "catalog:",
        "dotenv": "catalog:",
        "js-yaml": "catalog:",
        "winston-daily-rotate-file": "catalog:",
        "zod": "catalog:",
        "winston": "catalog:"
    },
    "devDependencies": {
        "@types/better-sqlite3": "catalog:",
        "@types/js-yaml": "catalog:",
        "@types/node": "catalog:",
        "typescript": "catalog:"
    }
}
