{
    "name": "@arete/backend",
    "private": true,
    "type": "module",
    "main": "dist/server.js",
    "scripts": {
        "build": "pnpm --dir ../.. --filter @arete/contracts run build && tsc -p tsconfig.json"
    },
    "dependencies": {
        "@arete/contracts": "workspace:*",
        "better-sqlite3": "catalog:",
        "dotenv": "catalog:",
        "js-yaml": "catalog:",
        "winston-daily-rotate-file": "catalog:",
        "zod": "catalog:",
        "winston": "catalog:"
    },
    "devDependencies": {
        "@types/better-sqlite3": "catalog:",
        "@types/js-yaml": "catalog:",
        "@types/node": "catalog:",
        "typescript": "catalog:"
    }
}
