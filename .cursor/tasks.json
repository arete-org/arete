{
  "commands": {
    "lint-fix": "pnpm lint:fix",
    "lint": "pnpm lint",
    "review": "pnpm review",
    "type-check": "pnpm type-check",
    "validate-tags": "pnpm validate-footnote-tags",
    "validate-openapi-links": "pnpm validate-openapi-links"
  },
  "tasks": {
    "lint-fix": {
      "command": "pnpm lint:fix",
      "description": "Apply formatting and lint fixes."
    },
    "lint": {
      "command": "pnpm lint",
      "description": "Run non-mutating lint checks."
    },
    "review": {
      "command": "pnpm review",
      "description": "Run project review checks."
    }
  }
}
