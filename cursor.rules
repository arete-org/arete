# Cursor Rules (Adapter)

Use `AGENTS.md` as the canonical guidance file for this repo.

If another instruction file conflicts with `AGENTS.md`, follow `AGENTS.md`.

Cursor-specific metadata belongs in `.cursor/*.json`.
Keep this file short on purpose.
