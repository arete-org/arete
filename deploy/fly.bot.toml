# Fly app: discord bot (no HTTP service)
app = 'footnote-discord'
primary_region = 'ord'

[build]
  dockerfile = 'Dockerfile.bot'

[env]
  NODE_ENV = 'production'
  BACKEND_BASE_URL = 'http://footnote-backend.internal:3000'

[processes]
  app = 'node dist/index.js'

[[vm]]
  cpu_kind = 'shared'
  cpus = 1
  memory = '256mb'
